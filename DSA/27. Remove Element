class Solution {
    public int removeElement(int[] nums, int val) {
        int index=0;
        for(int i=0; i<nums.length; i++){
            int first = nums[i];
            int second = val;
            if(first!=second){
                nums[index++]=first;
            }
        }
        return index;
    }
}
